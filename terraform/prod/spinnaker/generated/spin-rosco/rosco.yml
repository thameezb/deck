spectator:
  applicationName: ${spring.application.name}
  webEndpoint:
    enabled: false

server:
  port: ${services.rosco.port}
  address: ${services.rosco.host}
rosco:
  configDir: /opt/rosco/config/packer

# redis:
#   enabled: false
#   cache:
#     enabled: false
#   scheduler:
#     enabled: false
#   taskRepository:
#     enabled: false
redis:
  connection: ${services.redis.baseUrl:redis://localhost:6379}
